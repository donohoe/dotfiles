general {
    hide_cursor = true
}

background {
    monitor =
    color = rgba(111111ff)
}

label {
    monitor =
    text = cmd[update:1000] echo "$(date +'%H:%M')"
    font_size = 48
    position = 0, 80
    halign = center
    valign = center
}

label {
    monitor =
    text = $USER
    font_size = 18
    position = 0, 20
    halign = center
    valign = center
}

input-field {
    monitor =
    size = 300, 50
    position = 0, -50
    halign = center
    valign = center
    placeholder_text = Password
}

